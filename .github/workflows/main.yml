on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  update:
    runs-on: macOS-15
    steps:
      - run: '$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --list'
        name: 'List emulators'
